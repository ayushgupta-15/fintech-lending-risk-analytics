# Simulation Engine

The Policy Studio executes dynamic SQL simulations against the `gold.policy_engine` table. It models how changing DTI, Income, Grade, and Loan Amount thresholds affects portfolio approval rates and default probabilities.
