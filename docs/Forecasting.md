# Forecasting (Roadmap)

Forecasting models (like ARIMA or Prophet) for predicting portfolio default trends over time are planned for future iterations. Currently, the platform relies on static historical simulations.
