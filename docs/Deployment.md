# Deployment

The platform is designed to be fully containerized. A `docker-compose.yml` provides the database and data warehouse layer. The backend and frontend can be easily deployed to container orchestration services like AWS ECS, Vercel (for frontend), and Render (for backend).
